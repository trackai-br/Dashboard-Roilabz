---
phase: 02-add-guard-assertion-before-publish-bug-1b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/meta-ads-manager/src/components/campaign-wizard/PreviewPublishStep.tsx
autonomous: true
requirements:
  - BUG-1b

must_haves:
  truths:
    - "Se distribution.length != batch.totalCampaigns, a chamada POST /api/meta/bulk-publish NÃO é feita"
    - "O batch afetado aparece como 'failed' com mensagem contendo os dois valores (esperado e atual)"
    - "Se distribution.length == batch.totalCampaigns, o guard passa silenciosamente e a chamada ocorre normalmente"
    - "handleRetryBatch() tem o mesmo guard — mesma lógica, mesmo padrão de retorno"
    - "O TypeScript compila sem erros após a inserção"
  artifacts:
    - path: "apps/meta-ads-manager/src/components/campaign-wizard/PreviewPublishStep.tsx"
      provides: "Guard em handlePublish() e handleRetryBatch()"
      contains: "[bulk-publish] Guard failed"
  key_links:
    - from: "handlePublish() — linha após const distribution = distributionResult.entries"
      to: "updatePublishBatch + continue"
      via: "if (distribution.length !== expectedCount)"
      pattern: "distribution\\.length !== expectedCount"
    - from: "handleRetryBatch() — linha após const distribution = distributionResult.entries"
      to: "updatePublishBatch + return"
      via: "if (distribution.length !== expectedCount)"
      pattern: "distribution\\.length !== expectedCount"
---

<objective>
Inserir guard assertion em dois pontos de PreviewPublishStep.tsx para impedir que arrays de distribuição com tamanho incorreto cheguem à Meta API.

Purpose: Após a correção do BUG-1a (Phase 1), a distribuição deve ser sempre exata. Este guard captura qualquer regressão futura e impede silenciosamente que contagens erradas avancem para o POST.

Output: PreviewPublishStep.tsx com dois blocos de guard inseridos — um em handlePublish() com `continue`, outro em handleRetryBatch() com `return`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-add-guard-assertion-before-publish-bug-1b/02-RESEARCH.md

<interfaces>
<!-- Contratos relevantes extraídos do arquivo alvo — o executor não precisa explorar o codebase. -->

De PreviewPublishStep.tsx — estado atual após Phase 1:

```typescript
// handlePublish() — trecho relevante (linhas 134-144)
// INSERTION POINT: entre linha 134 e linha 136

          const distribution = distributionResult.entries;
          // ← INSERIR GUARD AQUI (indentação: 10 espaços)

          const res = await authenticatedFetch('/api/meta/bulk-publish', {
            method: 'POST',
            body: JSON.stringify({
              distribution,
              campaignConfig: batch.campaignConfig,
              adsetTypes: batch.adsetTypes,
              adConfig: { ...adConfig, creativeFiles: creativePool },
            }),
          });

// handleRetryBatch() — trecho relevante (linhas 203-204)
// INSERTION POINT: entre linha 203 e linha 204

      const distribution = distributionResult.entries;
      // ← INSERIR GUARD AQUI (indentação: 6 espaços)

      const res = await authenticatedFetch('/api/meta/bulk-publish', {
```

Padrão de erro já existente no arquivo (handlePublish, linha 126-132) — o guard DEVE espelhar este padrão:
```typescript
          if (distributionResult.error) {
            updatePublishBatch(batch.id, {
              status: 'failed',
              results: [{ campaignIndex: 0, status: 'failed', error: distributionResult.error }],
            });
            continue;
          }
```

Padrão equivalente em handleRetryBatch (linha 195-200):
```typescript
      if (distributionResult.error) {
        updatePublishBatch(batchId, {
          status: 'failed',
          results: [{ campaignIndex: 0, status: 'failed', error: distributionResult.error }],
        });
        return;
      }
```

Variáveis disponíveis em cada escopo:
- handlePublish(): `batch.id` (não `batchId`), `batch.totalCampaigns`, `distribution`
- handleRetryBatch(): `batchId` (não `batch.id`), `batch.totalCampaigns`, `distribution`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inserir guard em handlePublish()</name>
  <files>apps/meta-ads-manager/src/components/campaign-wizard/PreviewPublishStep.tsx</files>
  <read_first>
    - apps/meta-ads-manager/src/components/campaign-wizard/PreviewPublishStep.tsx — linhas 110-170 (handlePublish completo, ver ponto exato de inserção)
  </read_first>
  <behavior>
    - Quando distribution.length === batch.totalCampaigns: guard passa silenciosamente, sem nenhum efeito
    - Quando distribution.length !== batch.totalCampaigns: updatePublishBatch chamado com status 'failed' e error contendo "[bulk-publish] Guard failed: expected N campaigns, but distribution generated M entries" (N e M são os valores reais), seguido de continue para não abortar os outros batches
    - API call (authenticatedFetch) NÃO é executada quando guard falha
  </behavior>
  <action>
Inserir o bloco abaixo IMEDIATAMENTE APÓS a linha `const distribution = distributionResult.entries;` em handlePublish() e ANTES da linha `const res = await authenticatedFetch('/api/meta/bulk-publish', {`.

Indentação exata: 10 espaços (espelha o bloco `if (distributionResult.error)` acima).

Bloco a inserir:

```typescript
          // Guard: verify distribution count matches configured campaign count
          const expectedCount = batch.totalCampaigns;
          if (distribution.length !== expectedCount) {
            updatePublishBatch(batch.id, {
              status: 'failed',
              results: [{ campaignIndex: 0, status: 'failed', error: `[bulk-publish] Guard failed: expected ${expectedCount} campaigns, but distribution generated ${distribution.length} entries` }],
            });
            continue;
          }
```

Linha de referência para localização: a linha `const distribution = distributionResult.entries;` está na função handlePublish, dentro do bloco `for (const batch of batches)` → `try`. A linha seguinte original é `const res = await authenticatedFetch(...)`. O bloco deve ficar entre elas, com uma linha em branco separando do `const distribution` e outra do `const res`.
  </action>
  <verify>
    <automated>cd /Users/guilhermesimas/Documents/Dashboard/apps/meta-ads-manager && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo contém a string exata `[bulk-publish] Guard failed: expected` em handlePublish()
    - A linha `continue;` aparece dentro do bloco guard de handlePublish() (não em outro lugar)
    - A linha `const res = await authenticatedFetch('/api/meta/bulk-publish',` permanece após o guard — não foi movida nem removida
    - `npx tsc --noEmit` termina com exit code 0 (zero erros de TypeScript)
    - O bloco guard está posicionado APÓS `if (distributionResult.error)` e ANTES do `authenticatedFetch`
  </acceptance_criteria>
  <done>Guard inserido em handlePublish(). TypeScript compila sem erros. A string "[bulk-publish] Guard failed" aparece no arquivo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inserir guard em handleRetryBatch()</name>
  <files>apps/meta-ads-manager/src/components/campaign-wizard/PreviewPublishStep.tsx</files>
  <read_first>
    - apps/meta-ads-manager/src/components/campaign-wizard/PreviewPublishStep.tsx — linhas 175-236 (handleRetryBatch completo, ver ponto exato de inserção)
  </read_first>
  <behavior>
    - Quando distribution.length === batch.totalCampaigns: guard passa silenciosamente
    - Quando distribution.length !== batch.totalCampaigns: updatePublishBatch chamado com batchId (não batch.id), status 'failed', e mesma mensagem de erro; seguido de `return` (não `continue` — handleRetryBatch não está em loop)
    - API call NÃO é executada quando guard falha
  </behavior>
  <action>
Inserir o bloco abaixo IMEDIATAMENTE APÓS a linha `const distribution = distributionResult.entries;` em handleRetryBatch() e ANTES da linha `const res = await authenticatedFetch('/api/meta/bulk-publish', {`.

ATENÇÃO: nesta função a variável é `batchId` (parâmetro da função), NÃO `batch.id`.
ATENÇÃO: usar `return`, NÃO `continue` (não há loop for aqui).
Indentação exata: 6 espaços (espelha o bloco `if (distributionResult.error)` acima na mesma função).

Bloco a inserir:

```typescript
      // Guard: verify distribution count matches configured campaign count
      const expectedCount = batch.totalCampaigns;
      if (distribution.length !== expectedCount) {
        updatePublishBatch(batchId, {
          status: 'failed',
          results: [{ campaignIndex: 0, status: 'failed', error: `[bulk-publish] Guard failed: expected ${expectedCount} campaigns, but distribution generated ${distribution.length} entries` }],
        });
        return;
      }
```

Linha de referência: a linha `const distribution = distributionResult.entries;` em handleRetryBatch está após o bloco `if (distributionResult.error)` que usa `return`. A linha seguinte original é `const res = await authenticatedFetch(...)`.
  </action>
  <verify>
    <automated>cd /Users/guilhermesimas/Documents/Dashboard/apps/meta-ads-manager && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo agora contém DOIS blocos guard com a string `[bulk-publish] Guard failed: expected` — um em cada função
    - O guard em handleRetryBatch usa `batchId` (não `batch.id`) no `updatePublishBatch`
    - O guard em handleRetryBatch usa `return` (não `continue`)
    - A linha `const res = await authenticatedFetch('/api/meta/bulk-publish',` em handleRetryBatch permanece após o guard
    - `npx tsc --noEmit` termina com exit code 0
    - Grep confirma: `grep -n "Guard failed" apps/meta-ads-manager/src/components/campaign-wizard/PreviewPublishStep.tsx` retorna exatamente 2 linhas
  </acceptance_criteria>
  <done>Guard inserido em handleRetryBatch(). TypeScript compila sem erros. Exatamente 2 ocorrências de "[bulk-publish] Guard failed" no arquivo — uma por função.</done>
</task>

</tasks>

<verification>
Após ambas as tasks:

```bash
# 1. TypeScript compila sem erros
cd /Users/guilhermesimas/Documents/Dashboard/apps/meta-ads-manager && npx tsc --noEmit

# 2. Exatamente 2 guards inseridos (um por função)
grep -n "Guard failed" apps/meta-ads-manager/src/components/campaign-wizard/PreviewPublishStep.tsx

# 3. Guard em handlePublish usa `continue` (loop)
grep -A 8 "Guard failed" apps/meta-ads-manager/src/components/campaign-wizard/PreviewPublishStep.tsx | grep -c "continue"

# 4. Guard em handleRetryBatch usa `return`
grep -A 8 "Guard failed" apps/meta-ads-manager/src/components/campaign-wizard/PreviewPublishStep.tsx | grep -c "return"

# 5. authenticatedFetch ainda presente em ambas as funções (não foi removido)
grep -c "authenticatedFetch" apps/meta-ads-manager/src/components/campaign-wizard/PreviewPublishStep.tsx
```

Resultado esperado:
- tsc: exit 0, nenhum erro
- grep Guard failed: 2 linhas
- grep continue dentro dos guards: 1
- grep return dentro dos guards: 1
- grep authenticatedFetch: 2 (um por função)
</verification>

<success_criteria>
1. PreviewPublishStep.tsx contém guard em handlePublish() com `updatePublishBatch + continue`
2. PreviewPublishStep.tsx contém guard em handleRetryBatch() com `updatePublishBatch + return`
3. Ambos os guards usam a mensagem exata: `[bulk-publish] Guard failed: expected ${expectedCount} campaigns, but distribution generated ${distribution.length} entries`
4. `npx tsc --noEmit` passa sem erros
5. O fluxo normal (distribution.length === totalCampaigns) não é afetado — guard é transparente quando contagens batem
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-add-guard-assertion-before-publish-bug-1b/02-01-SUMMARY.md` com:
- Arquivos modificados e linhas inseridas
- Resultado do `tsc --noEmit`
- Resultado do `grep "Guard failed"`
- Estado final: BUG-1b resolvido
</output>
